export type Resume = { resumeId: string; fileName: string; uploadedAt: string | null; indexStatus: string; skills: string[] };
export type HistoryItem = { id: string; resumeName: string; atsScore: number; createdAt: string };
